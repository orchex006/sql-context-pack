# Codex packaging

Use the root `.codex-plugin/plugin.json` and canonical `skills/sql-context-pack/SKILL.md`. Configure the owner-started MCP URL and agent token through protected harness configuration; never paste either database credentials or the owner-control credential into a prompt.
